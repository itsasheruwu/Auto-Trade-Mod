## Auto Trade Mod v1.0.6

### Features
- Added `Auto-close UI when out of input items` setting.
- When `Keep auto ON when trading UI closes` is enabled, Auto can now close the villager trading screen after sustained missing-input state.

### Behavior
- Auto-close only triggers for missing input items (not disabled trades), after a short stability window to avoid one-tick desync closes.
